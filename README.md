1. LEOKA: deals wiith the Leoka dataset where I calculate a few problems and make 2 descriptive bar plots.

2. NIBRS: (Assignment 2) deals with the NIBRS data (National Incident-Based Reporting System), where I create a lookup table for UCR offense codes, Find all crimes in Philadelphia for which the reporting agency was the Philadelphia Police
Department and make a table of the crime types, subset the property data to contain only mobile phones, figure out where the homicide rate is the highest.

3. NCVS: National Crime Victimisation Survey, where the focus was on the rigorous application of the "SERIES_WEIGHT" variable to ensure that results reflect the actual U.S. population rather than a biased survey sample. Explored the context of assaults by examining where and when crimes occur, the characteristics of offenders, the types of weapons used, and the frequency of defensive firearm use by victims. The second part dealt with demographic assessment of victimisation rates across various crime types by generating weighted estimates of the total number of victims by race and crime category.

4. Regular Expressions (PART 1): RegEx, I performed pattern matching and frequency analysis on a dataset of 4,030 four-letter Scrabble words. Using R functions like scan() and grep(), the script identifies specific word structures.

5. Regular Expressions (PART 2): 

6. SQL Practice: Chicago Crime Data | This project focuses on executing and optimising SQL queries within a SQLite environment using Chicago crime data. The workflow begins by establishing a database connection and verifying the schema integrity, ensuring that tables like crime and iucr are correctly formatted with standardized timestamps and excluding redundant fields. The primary objective is to demonstrate proficiency in relational database management by using SQL to perform precise data extraction and aggregation. I count total incident volumes, perform targeted lookups by case numbers or locations, and filter for specific time windows like business hours. By joining the crime and iucr tables, the script resolves human-readable primary types for specific offenses and aggregates motor vehicle thefts by ward. These tasks showcase the power of SQL for managing large-scale public safety datasets and generating localized reports on criminal activity.
   
7. SQL and Hotspot Mapping: I built a SQLite database for Los Angeles crime incidents, the dataset is pushed directly from a source CSV into the SQL environment without loading the entire file into R, simulating the handling of datasets too large for local memory. I then extracted coordinates which have been used to generate a professional-grade hotspot map that identifies areas of high criminal density across the city.



